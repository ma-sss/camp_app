<!DOCTYPE html>
<html>
  <head>
    <title>CampApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <ul class="navbar-nav me-auto">
          <a class="navbar-brand" href="/">TOP(キャンプ場一覧)</a>
          <% if current_user %>  
            <li class="nav-item">
              <a class="nav-link" href="/reservations/user_reservations">ユーザー予約状況</a>
            </li>
            <li class="nav-item">
              <a><%= link_to "お気に入り一覧", favorites_path, class:"nav-link" %></a>
            </li>
            <li class="nav-item">
              <a><%= link_to "ログアウト",destroy_user_session_path,data: { turbo_method: :delete }, class:"nav-link" %></a>
            </li>
          <% else %>
            <li class="nav-item">
              <a><%= link_to "新規作成",new_user_registration_path, class:"nav-link" %></a>
            </li>
            <li class="nav-item">
              <a><%= link_to "ログイン画面へ",new_user_session_path, class:"nav-link" %></a>
            </li>
          <% end %>
        </ul>
        </div>
      </div>
    </nav>

  </header>


  <body>
    <%= yield %>
  </body>
</html>
